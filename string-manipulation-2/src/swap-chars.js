/* eslint-disable no-unused-vars */

function swapChars(firstIndex, secondIndex, string) {
  let firstSwap = '';
  const output = '';
  firstSwap = string.replace(string.charAt(firstIndex), string.charAt(secondIndex));
  firstSwap.charAt(secondIndex).replace(string.charAt(firstIndex));
}
